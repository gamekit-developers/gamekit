# ---------------------------------------------------------
cmake_minimum_required(VERSION 2.6)


file(GLOB bParse_SRC "*.cpp")
file(GLOB bParse_HDR "*.h")
file(GLOB bParse_API "autogenerated/*.h")

add_library(${OGREKIT_BPARSE_TARGET} ${bParse_SRC} ${bParse_HDR} ${bParse_API})